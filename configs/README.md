# Configuration catalog

The executable catalog is `mmw.config.loadExperiment`. These directories
document the three independent configuration dimensions and are reserved for
future JSON/YAML serialization. Algorithms never read files from these folders.

- `experiments/`: named experiment IDs and expected research interpretation
- `arrays/`: radar placement families
- `waveforms/`: carrier and chirp families
